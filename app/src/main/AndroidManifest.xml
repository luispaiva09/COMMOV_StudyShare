<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudyShare"
        tools:targetApi="31">

        <activity
            android:name=".Activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Activities.RegisterActivity"
            android:exported="true" />

        <activity
            android:name=".Activities.InicioActivity"
            android:exported="true" />

        <activity
            android:name=".Activities.AddMaterialActivity"
            android:exported="true" />

        <activity
            android:name=".Activities.AddCategoriaActivity"
            android:exported="true" />

        <activity
            android:name=".Activities.BaseActivity"
            android:exported="true" />

        <activity
            android:name=".Activities.PerfilActivity"
            android:exported="true"
            android:enableOnBackInvokedCallback="true"/>

        <activity
            android:name=".Activities.MyMateriaisActivity"
            android:exported="true" />

        <activity
            android:name=".Activities.MaterialDetalheActivity"
            android:exported="true" />

        <activity
            android:name=".Activities.AddDiscussaoActivity"
            android:exported="true" />

        <activity
            android:name=".Activities.MyDiscussoesActivity"
            android:exported="true" />

        <activity
            android:name=".Activities.DiscussaoDetalheActivity"
            android:exported="true" />

        <activity
            android:name=".Activities.AddSessaoEstudoActivity"
            android:exported="true" />

    </application>

</manifest>
